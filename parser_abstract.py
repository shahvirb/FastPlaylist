class Parser:
    # Returns parsed data (dict of keys) for all rows
    def parse(self, soup):
        return None
        
    # Must return a list of keys which it will parse in each row
    @property
    def keys(self):
        return None
